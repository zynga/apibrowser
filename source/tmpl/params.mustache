<span class="params">
	{{?params}}
		(
		{{#params}}
			{{?position}}, {{/position}}
			<span class="name">{{name}}</span>
			{{>type}}
		{{/params}}
		)
	{{/params}}
	{{^params}}
		()
	{{/params}}
</span>