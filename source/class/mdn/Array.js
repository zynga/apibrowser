/** 
 * Creates a regular expression object for matching text with a @pattern {String} and optional @flags {String}. 
 * 
 * If the only argument passed to the Array constructor is an integer between 0 and 232-1 (inclusive), 
 * a new JavaScript array is created with @arrayLength {Integer} number of elements. 
 * If the argument is any other number, a RangeError exception is thrown.
 *
 * See also: [MDN Documentation](https://developer.mozilla.org/en/JavaScript/Reference/Global_Objects/Array)
 *
 * #main
 */
core.Main.declareNamespace("Array", function(arrayLength) {});
